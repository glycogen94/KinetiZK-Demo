<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.KinetiZKDemo" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>
    
    <!-- Splash Screen Theme -->
    <style name="Theme.KinetiZKDemo.Splash" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- 스플래시 화면 배경색과 동일하게 상태 표시줄 색상 설정 (SplashActivity.kt에서도 설정하고 있음) -->
        <item name="android:statusBarColor">@color/background_dark</item>
        <!-- 필요에 따라 windowBackground 등을 설정할 수 있지만, activity_splash.xml에서 이미 배경을 설정하고 있음 -->
        <!-- <item name="android:windowBackground">@color/background_dark</item> -->
        <!-- <item name="android:windowFullscreen">true</item> -->
    </style>

</resources>